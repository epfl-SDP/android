package ch.epfl.sdp.mobile.application.tournaments

/**
 * A class which represents a reference to a tournament.
 *
 * @property uid the unique identifier of the tournament.
 */
data class TournamentReference(val uid: String)
